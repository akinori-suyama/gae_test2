machine:
  timezone:
    Asia/Tokyo
machine:
  python:
    version: pypy-2.2.1
dependencies:
  pre:
    - pip install PIL --allow-external PIL --allow-unverified PIL
test:
  override:
    - exit 0
deployment:
  appengine:
    branch: master
    commands:
      -  fab deploy

